/** Converts an iterable of bytes into an array of individual bits. */
export declare function bytesToBits(bytes: Iterable<number>): number[];
/** Converts an array of bits back into a Uint8Array of bytes. */
export declare function bitsToBytes(bits: number[]): Uint8Array;
