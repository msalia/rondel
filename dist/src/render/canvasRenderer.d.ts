import type { EncodedCode } from "../types";
/** Renders an encoded circular code onto an HTML canvas element. */
export declare function renderCanvas(code: EncodedCode, size?: number): HTMLCanvasElement;
